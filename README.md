# dirty little helpers

> A collection of helper scripts for my FreeBSD machines

Please see the `README.md` files in the subfolders.

## Remarks

- `.sh` scripts
  - Are written to be portable
    - Do not rely on `bash`
  - Use full paths for commands so they can run inside crontab
- `.py` scripts
  - Should work on the latest stable `python3` version on FreeBSD
